language: c

os:
  - linux
  - osx

compiler:
  - clang
  - gcc

addons:
  apt:
    packages:
      - build-essential
      - clang-format
      - valgrind

script:
  - make -j4
  - sh -c ./scripts/tests.sh

notifications:
  email:
    on_success: change
    on_failure: always
