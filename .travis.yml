language: c

os:
  - linux
  - osx

compiler:
  - clang
  - clang-3.6
  - gcc
  - gcc-5

addons:
  apt:
    packages:
      - build-essential
      - clang-format
      - valgrind

script:
  - make -j4
